alias bt_on='bt_enable';alias bt_off='bt_disable';bt_enable(){ sudo modprobe btusb;sudo rfkill unblock bluetooth;sudo service bluetooth start;};bt_disable(){ sudo service bluetooth stop;sudo rfkill block bluetooth;sudo rmmod btusb;};bt_reset(){ bt_disable;bt_enable;for device in $(bt_getdevices);do sudo hciconfig "$device" down;sudo hciconfig "$device" reset;sudo hciconfig "$device" up;done;};bt_getdevices(){ sudo hciconfig|awk -F':' '/^\w/{print $1}';};bt_getconfig(){ sudo hciconfig;sudo hcitool dev;sudo hcitool scan;};[ "${1#bt}" != "$1" ]&&"$@"||true