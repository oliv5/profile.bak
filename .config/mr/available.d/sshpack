[${XDG_CONFIG_HOME:-$HOME/.config}/vcsh/repo.d/sshpack.git]
skip     = [ ! -d $HOME/private/sshpack ]
checkout = vcsh_clone_remote
annex    = annex_sync
gitk     = gitk
pull     = git_pull rpi master
push     = git_push rpi master
gc       = git gc
bundle   = git_bundle
