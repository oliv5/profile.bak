[${XDG_CONFIG_HOME:-$HOME/.config}/vcsh/repo.d/profile.git]
bundle   = git_bundle "${PRIVATE:-$HOME}/repo/sync/git/bundle";
           annex_bundle "${PRIVATE:-$HOME}/repo/sync/git/bundle"

pull     = git_pull "rpi github" master
push     = git_push "rpi github" master
sync     = annex_sync; repo sync

download = annex_copy . --from hubic --auto
upload   = annex_copy . --to hubic --auto

status   = git status -s && annex_status
diff     = annex_diff
gdm      = git difftool -y -t meld --

gitk     = gitk
gc       = git gc
meta     = git_meta
