[${PRIVATE:-$HOME}/media/download]
skip     = ! git --git-dir="$MR_REPO/.git" rev-parse --verify "HEAD" >/dev/null 2>&1
checkout = annex_clone ssh://olivier@rpi:/media/wdtv/download rpi 2>/dev/null || 
           annex_clone ssh://olivier@oliv5kta.dtdns.net:443/media/wdtv/download rpi
status   = annex_status
sync     = annex_sync
gitk     = gitk
gdm      = git difftool -y -t meld --
gc       = git gc
diff     = annex_diff
