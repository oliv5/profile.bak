[${PRIVATE:-$HOME}/media/download]
skip     = [ ! -e "$MR_REPO" ] || ! git --git-dir="$MR_REPO/.git" rev-parse --verify "HEAD" >/dev/null 2>&1
checkout = annex_clone_direct ssh://olivier@oliv5kta.dtdns.net:443/media/wdtv/media/download rpi
sync     = annex_sync
gitk     = gitk
pull     = git_pull rpi master
push     = git_push rpi master
gc       = git gc
