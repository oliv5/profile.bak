#!/bin/sh
set -e
if [ "x$SSH_ORIGINAL_COMMAND" != "x" ]; then
exec /opt/git-annex.linux/runshell git-annex-shell -c "$SSH_ORIGINAL_COMMAND"
else
exec /opt/git-annex.linux/runshell git-annex-shell -c "$@"
fi
